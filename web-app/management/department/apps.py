"""Application registration module"""

from django.apps import AppConfig


class DepartmentConfig(AppConfig):
    """In the class, you must specify the name of the registered application"""

    name = 'department'
